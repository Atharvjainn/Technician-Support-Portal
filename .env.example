# Copy .env.example to .env.local and add your free Groq key from console.groq.com to enable live AI responses. Without it, the app falls back to the scripted mock expert automatically.

GROQ_API_KEY=your_groq_api_key_here